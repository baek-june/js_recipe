// DOM 엑세스 타이밍에 처리 실행
const boxNum = document.querySelectorAll('.box').length;
console.log(`.box 요소의 개수는 ${boxNum}개입니다.`);

// window.addEventListener('DOMContentLoaded', () => {
//   const boxNum = document.querySelectorAll('.box').length;
//   console.log(`.box 요소의 개수는 ${boxNum}개입니다.`);
// });